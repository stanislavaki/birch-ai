# Версионирование юридических документов (`/legal/*`)

План переноса юр-документов из статичных страниц в CMS-коллекцию, чтобы у каждого документа
была история редакций: актуальная всегда по адресу без даты, прошлые — по адресу с датой,
переключение через селектор на строке `Effective date`.

Статус на 23.09.2026: **пять документов залиты и сверены, шаблон собран и проверен на стейджинге.**
Катовер не начинался. 23.09.2026 пришла вводная про релиз 27 сентября — схема расширена статусами
редакций и записью-указателем, см. отдельный раздел ниже.

| Сущность | Id |
|---|---|
| Коллекция `Legal document types` | `6ab28b9c59aafa375acd46ac` |
| Коллекция `Legal documents` | `6ab28b9f2caaa5ecf2d28799` |
| Тип Terms / Privacy / Cookies / Affiliate / DPA | `…d235` / `…d237` / `…d239` / `…d23b` / `…d23d` (префикс `6ab28d53ca76ba500094`) |
| Записи документов: terms / privacy / cookies / affiliate / dpa | `6ab290b8deabe45960bf044e` / `6ab290ba16d487ae500d063e` / `6ab28d99b88163f93b0c6125` / `6ab290bc34b833ea134714f1` / `6ab290bde1f153ada57f9b30` |

Слаги коллекций временные (`legal-documents`, `legal-document-types`) — постоянный `legal`
отдаётся только на катовере, см. «Порядок работ». Записи держим черновиками (`isDraft: true`),
пока не собран шаблон: иначе ближайшая полная публикация кем угодно из команды выложит
дубли юр-документов по адресам `/legal-documents/*`.

## Как устроено сейчас

Шесть статичных страниц в папке `legal` (`6716718ea408f53194adfac2`), сайт `6716718ea408f53194adf9a9`:

| Страница | `page_id` | URL | Effective date | Тело |
|---|---|---|---|---|
| Terms and Conditions | `6716718ea408f53194adfafe` | `/legal/terms` | October 15, 2024 | rich text 24 КБ |
| Privacy Policy | `6716718ea408f53194adfae2` | `/legal/privacy` | June 8, 2026 | rich text 32 КБ |
| Cookie Policy | `6716718ea408f53194adfa58` | `/legal/cookies` | June 8, 2026 | rich text 7 КБ + таблица |
| Data Processing | `68d6608f5f68aa2c38c2d290` | `/legal/data-processing` | June 8, 2026 | rich text 58 КБ + таблица |
| Referral Program T&C | `6716718ea408f53194adfae4` | `/legal/referral-program-terms-and-conditions` | October 15, 2024 | rich text 7,6 КБ |
| Opt Out | `6a392964f7734eba5c2e8613` | `/legal/opt-out` | — | **не документ этого семейства** |

Структура тела одинаковая у всех пяти документов:

```
.centered-terms (max-width 800px, центрируется)
  .legal-menu-tabs > .tabs-cont > a.legal-menu-link.tab2   ← табы между документами
  p._14px.grey                                             ← «Effective date: …» ← сюда селектор
  .legal-rt.w-richtext                                     ← тело
  [.w-embed > .cookie-table-wrap > table]                  ← таблицы: отдельный HTML embed
  [.legal-rt.w-richtext]                                   ← продолжение тела после таблицы
```

`Opt Out` — лендинг с формой и canvas-анимацией, без даты и вне табов. В систему не входит.

### Что проверено по содержимому (важно для переноса)

- **Теги внутри rich text:** только `p`, `h3`, `h5`, `h6`, `ul`/`li`, `a`, `strong`, `em`, `br`.
  Всё это родное для CMS rich text — тела переезжают без разбора и пересборки.
- **Ни одного `id`-якоря и ни одной ссылки на `#раздел`** ни в одном документе. Это была главная
  опасность переезда (rich text не даёт ставить `id` на заголовки) — её нет.
- **Таблицы уже лежат в HTML embed**, а не в rich text. В CMS embed вставляется внутрь
  rich-text-поля (плюсик на новой строке в CMS-редакторе → HTML embed), то есть переезжает один-в-один.
  Формулировка «CMS rich text не умеет таблицы» неверна: нет *элемента* «таблица», но embed внутри поля есть.
- **Размеры:** максимум — DPA, 62 КБ после сборки тела. Лимит текстового поля Webflow порядка
  мегабайта, запас кратный.
- **Таблицы кладутся в тело обёрткой `<div data-rt-embed-type='true'>`** — см. `webflow-mcp.md`.
  Первая заливка ушла с `<div class="w-embed">` (разметка статичной страницы), и редактор показал
  таблицу простынёй текста вместе с CSS. Data API при этом вернул отправленное байт в байт: **эхо
  ответа не доказывает, что разметка распознана**, проверять надо в редакторе или на странице.
  **CSS в записи не лежит вообще.** Запись держит содержание, структуру и ширины колонок инлайном
  на `<col>`; всё общее (рамки, паддинги, кегль, скролл на мобилке) — один раз в кастом-коде
  шаблона, каноника в [`webflow/legal-template.head.html`](../webflow/legal-template.head.html).
  Обёртка `<div class="rt-table">` — единственное имя класса, которое несёт разметка; на сайте оно
  свободно, чужие таблицы (`.uc-table`, `.table` в use-cases) не заденет.

  Первоначально CSS оставался внутри embed'а «чтобы архивная редакция была самодостаточной» — это
  неверный размен: замораживать надо текст, а не толщину рамок. Иначе через пару лет на сайте живёт
  несколько поколений таблиц, и ни одну нельзя поправить разом. Два исходных блока CSS оказались
  идентичны с точностью до имён классов, ширин колонок и мобильного `min-width` (680 против 640,
  сведено к одному значению).
- **Заливка идёт мимо контекста агента.** Тела нельзя гнать через tool-вызовы: агент физически
  набирает 62 КБ юридического текста, и потерянную оговорку потом никто не заметит. Рабочая схема:
  отдельный Webflow API-токен с правами CMS read+write лежит в `~/.webflow-token` (не в репозитории,
  не в переписке), скрипты в скретчпаде читают страницы, собирают тела и шлют их напрямую в Data API.
  Существующий `sms-sites-token` не трогать: он read-only и на нём висит чужая живая интеграция.

## Решения (согласовано 22.09.2026)

- **Полный CMS.** Одна коллекция, и текущие, и архивные редакции — записи в ней. Причина: статичные
  архивы замораживают не только текст, но и обвязку (табы, селектор, типографика). Через три года это
  ~20 страниц, и любая правка стиля — двадцать раз руками.
- **Архив копится с сегодняшнего дня.** Текстов прошлых редакций нет, на старте по одной записи на
  документ, все `Is current`.
- **Архивы закрыты от индексации** (решение пересмотрено 22.09.2026). Сначала договорились не
  прятать и защищаться баннером — потому что поштучной настройки индексации в Webflow нет, а
  обходиться скриптом было дорого. Цена упала, когда появилось поле `Is archived`: теперь условие
  выражается видимостью, а она вырезает разметку целиком, так что скрипт внутри баннера существует
  только на архивных страницах. Механика — в `webflow-mcp.md`. Баннер при этом остаётся: он для
  того, кто пришёл по прямой ссылке, а `noindex` — чтобы недействующая редакция не всплывала
  в выдаче по запросу вроде «birch terms».
- **Селектор показывает все редакции**, текущая с пометкой.
- **Полей `summary of changes` и страницы-хаба нет.** Хаб (список всех редакций всех документов)
  нужен только под комплаенс-сценарий — аудитор просит историю изменений. Заводится за полчаса
  поверх той же коллекции, когда попросят.
- **`/legal/opt-out` → `/opt-out`** с 301, если префикс `legal` иначе не отдаётся коллекции.

### Как сверялся перенос (22.09.2026)

Две независимые проверки, обе прошли на всех пяти документах:

1. **Передача.** После заливки тела выкачаны обратно из CMS и сравнены с отправленными посимвольно —
   совпали (24 428 / 32 467 / 10 854 / 7 576 / 61 817 знаков).
2. **Сборка.** Видимый текст исходной страницы (без табов и строки `Effective date`) сравнён с
   видимым текстом собранного тела — совпал до знака (23 004 / 31 260 / 8 767 / 6 780 / 57 830),
   число ссылок и ячеек таблиц не изменилось.

Первая проверка одна ничего не доказывает: она сверяет отправленное с полученным, а не с оригиналом.
Потерю на этапе сборки ловит только вторая.

## Шаблон `Legal documents Template`

Страница `6ab28b9f2caaa5ecf2d287c5` (отдаёт `/legal-documents`, на катовере станет `/legal`).
Собран по **новой конвенции сайта** (section → container → row → col, распорки `.divider`),
а не по образцу статичной `/legal/privacy` — у той классы дореформенные (`old-container`, `_1100`,
`section-hero`, `_14px.grey`). Схема конвенции — в `webflow-mcp.md`.

```
Body                                     6ab28b9f2caaa5ecf2d287cf
├ компонент Fluid Type
└ .page-wrapper                          f0dcd868-c1ca-ca62-e7f8-c022b8007f4e
  ├ компонент super-navigation-component
  ├ main.page-main#main                  6734cac8-7eb1-6f60-f8c5-4123ed0c91cc
  │ ├ section.section                    58b7dfbc-99d2-4287-132f-126a23aa9486
  │ │   ├ .divider.s                     32753688-c0c1-a3ee-047a-c81f84778076
  │ │   ├ .container.centered            9c0ec6ce-8279-64a5-b790-70b1ac27753a
  │ │   │   ├ компонент legal-menu-tabs
  │ │   │   ├ h1.h1                      4cc8e16d-30c6-3793-16f0-0d8b47db55c8  → bind text = name
  │ │   │   └ p.paragraph-sm.u-text-midgray  b2d64346-819d-1d77-2109-e07bfa80e2a7  ← сюда селектор
  │ │   └ .divider.s                     3eeda614-0e84-87ba-08d7-083c789e097e
  │ └ section.section                    5b672736-7b41-635e-0a89-fdf5e00d24d7
  │     ├ .container.centered            e0e345f3-9103-16c1-ceb0-7112a47844e9
  │     │   └ RichText .rich-text-base.rt-legal  2447615e-c9f9-e7be-22bf-02520442cc5e → bind richText = body
  │     └ .divider.m                     5f9f5071-4905-a7bd-b608-bf39061c5de1
  └ компонент Global / Footer Black
```

Решения по раскладке (согласовано 22.09.2026):

- **Шапка во всю ширину `.container`** (1408px), текст — своей колонкой. Вариант «всё в одной колонке»
  отклонён. Прежняя страница держала шапку в 1100px, так что верхний блок стал шире на ~300px.
- **Ширина колонки текста независима от `u-measure`** — намеренно, чтобы правка системного токена
  не двигала юр-документы. Значение `34.66rem` (610px): при теле 0,9rem это ~75 знаков в строке.
  Исходные 680px считались в паре с укрупнением тела до 1rem; кегль решили не трогать, поэтому 610.
- **Заголовок — тег `h1` с классом `.h2.u-position-relative`** (решено 22.09.2026 после просмотра
  в Figma). Дефолтный `.h1` давал 76,8px на окне 1280 — для юр-документа слишком парадно; `.h2`
  даёт 54,7px, что близко к 52px, которые дизайнер поставила в макете. Тег остаётся `h1`: меняется
  только визуальный класс, структура заголовков страницы не ломается. Комбо `u-position-relative`
  обязательно — базовый `.h2` абсолютно позиционирован, см. `webflow-mcp.md`.
- **Ритм.** Сверху секции распорка `.divider.s` (70px), снизу тела — `.divider.m`. А два отступа
  вокруг шапки — над заголовком и под строкой даты — заданы **паддингом на группе**
  `legal-head-group`, а не распорками: одно значение `3rem` в одном месте вместо двух элементов
  (решение дизайнера 22.09.2026). На 1280 это 48px; корневой кегль на сайте плавающий, поэтому
  на 1600+ те же `3rem` дают 57,6px — отступ масштабируется вместе с типографикой.
- **`main.page-main#main`** — тег `main` и id проставлены, так что на юр-страницах skip-link наконец
  работает. На остальном сайте он сломан, заведено отдельной задачей.

**Табы уже компонент** (`legal-menu-tabs`, без пропсов) — ряд не надо собирать из коллекции
`Legal document types`, он и так в одном месте и ссылается на `/legal/*`, что после катовера верно.

Комбо `.rich-text-base.rt-legal` заведено, несёт только `max-width: 38.6rem` (≈680px при корне 17,6px).

Порядок сборки через MCP: `data_style_tool > create_style` (комбо) → `data_element_builder`
(секции деревом, `children[]`) → `data_component_builder` (инстансы компонентов по имени).
Билдер элементов инстансы ставить не умеет — для них отдельный компонентный билдер.

### Селектор редакций (собран 22.09.2026)

Строка даты: `.legal-eff` (flex, по центру) = подпись `Effective date:` обычным текстом +
нативный Dropdown. Кликается только дата со стрелкой, подпись остаётся текстом.

Отступы взяты с `.filter-tab` со страницы `/how-to-choose-birch-ai-tools`: паддинг `1rem`,
радиус `0.75rem`, gap `0.375rem`, серый `#F1F0F7`. Список повторяет логику самого компонента
табов — серая подложка, активная строка белая.

- `legal-ver` — обёртка (`margin: 0`, иначе вебфлоувский `auto` растаскивает ряд)
- `legal-ver-toggle` — триггер; серый на `:hover`, `:focus-visible` и `.w--open`
- `legal-ver-list` — список; **без `display`** в классе, иначе раскрыт всегда. Фон и размытие
  взяты у выпадающего меню нава: `rgba(211, 209, 224, 0.4)` + `backdrop-filter: blur(30px)
  saturate(0.8)`. Вебкит-префикс Webflow дописывает сам — задавать его руками не нужно, тул такое
  свойство и не принимает. `margin-top: 0.25rem` отделяет список от триггера.
- `legal-ver-item` — строка; радиус **4px**, не 12: внутренний радиус сопрягается с внешним по
  правилу «внешний минус паддинг» (12 − 8 = 4), иначе углы строки и панели не совпадают по кривизне
  и это видно. Шаг между пунктами 8px. Ховер и фокус дают полупрозрачный белый (50%), открытая редакция —
  сплошной белый по классу `.w--current`, который Webflow ставит сам. Покрасить его состоянием
  в панели стилей **не вышло**: в списке состояний ссылки есть «Current Locale», и правило из него
  в опубликованный CSS не попадает. Поэтому строка живёт в кастом-коде шаблона рядом с `.w--open`.
- `legal-eff-label` — комбо на подписи «Effective date:»: тот же вертикальный паддинг, что у плашки,
  и обнулённые маргины. Нужно для совпадения базовых линий, разбор — в `webflow-mcp.md`.

**Решения по разметке селектора (22.09.2026):**

- **Маркера «действующая редакция» в селекторе нет.** В макете была точка 6×6 рядом с датой, но её
  сняли: она путается с состоянием «открытая страница», а признаков в одном списке должно быть не
  больше одного. Какая редакция в силе, сообщает баннер архивной страницы со ссылкой на неё —
  информация не теряется, а селектор отвечает ровно на один вопрос: «где я».
- **Состояний строки два:** ховер и фокус — белый на 50% прозрачности, открытая редакция — сплошной
  белый через нативное `w--current`.

**Баннер архивной редакции** `legal-archive-note` над телом: ширина текстовой колонки (34.66rem),
серый `#F1F0F7`, паддинг 1rem, радиус 0.75rem, текст по левому краю. Видимость — по `Is archived`.

Текст одной строкой: «You're viewing an archived document. **See the current version →**». Даты
в баннере нет намеренно — она стоит прямо над ним, в селекторе.

**Цвет ссылки — `Secondary/Indigo` (#4551f2), без подчёркивания.** Токен с именем «синий» в палитре
есть — `Secondary/Lignt Blue` #59dbff, — но на сером фоне баннера он даёт контраст **1,43:1** при
пороге AA 4,5:1, то есть нечитаем. Indigo даёт 5,0:1. Правило на будущее: цвет ссылки выбирать
не по имени токена, а по замеру контраста на том фоне, где он окажется.

Ссылка живёт внутри отдельного Collection List (сортировка по дате вниз, лимит 1), потому что ведёт
на **другую** запись — действующую редакцию того же документа. Этому списку **нужен фильтр по типу
документа**, иначе он покажет самую свежую редакцию по всей коллекции, а не по этому документу.

**Поле `Is current` заменено на `Is archived`** (22.09.2026; 23.09.2026 заменено на `Status`, см.
«Статусы редакций и запись-указатель» — там же причина, почему одно поле, а не набор свитчей). После отказа от точки у `Is current`
не осталось ни одного потребителя, а условная видимость через API умеет только «показать, когда
свитч включён» — отрицания нет. Перевёрнутое по смыслу поле делает условие баннера выразимым
через API, то есть воспроизводимым при пересборке шаблона, без настройки, которую надо помнить.

**Документ с одной редакцией.** Селектор в этом случае бессмысленен, поэтому превращается в обычный
текст: `:has(.w-dyn-item:only-child)` гасит стрелку и снимает кликабельность. Условия «в списке
меньше двух элементов» в Webflow нет — считать он не умеет, поэтому это CSS. Вертикальный паддинг
при этом сохраняется: на нём держится совпадение базовых линий с подписью.

**Сделано руками в Designer** (через MCP невозможно, см. `webflow-mcp.md`): режим ссылки «текущий
элемент коллекции» и фильтр списка редакций по типу документа текущей записи.

Для баннерного списка то же самое сделано отдельно — **настройки живут на элементе, а не на
странице**, поэтому новый Collection List и новая ссылка не наследуют то, что настроено у селектора.
Проверено по факту: с `/legal-documents/cookies-2024-10-15` ссылка ведёт на `/legal-documents/cookies`.

**Тестовые данные:** запись `cookies-2024-10-15` (`6ab2c3e1e2a56ee92446513d`) заведена только чтобы
посмотреть, как расходятся страницы архива и актуальной редакции. Тело у неё то же, что у действующей.
**Удалить на катовере** (шаг 6).

**Заголовок архивной редакции — без пометки** (решено 22.09.2026): дубликат сохраняет заголовок
оригинала, дописывать ничего не нужно. Пометка с датой задумывалась, чтобы две одинаковые строки
не путались в выдаче, но после закрытия архивов от индексации в выдаче их нет вовсе — она стала
косметикой ценой ручного шага в регулярном процессе.

**SEO и OG.** `seo.title` и `seo.description` шаблона привязаны к полям `meta-title` и
`meta-description` (синтаксис плейсхолдера — в `webflow-mcp.md`). Картинка соцсетей одна на все
юр-страницы, поэтому стоит статикой на шаблоне (`68f8d64b607b39f592328399`), а поле `og-image`
в коллекции удалено за ненадобностью.

Описаний у старых страниц не было **ни у одной** — написаны с нуля, 133–159 знаков, по схеме
«что за документ и что в нём искать», с упоминанием Birch. Заголовки перенесены со старых страниц
как есть; у DPA он отличается по схеме от остальных («Data Processing | Birch» против
«<Название> | Birch (Revealbot)») — наследие, решить отдельно. У архивной редакции заголовок
получает пометку: «Cookie Policy (archived October 15, 2024) | Birch (Revealbot)».

## Схема коллекции `Legal documents`

| Поле | Тип | Назначение |
|---|---|---|
| `Name` | — | «Terms and Conditions» |
| `Slug` | — | `terms` у актуальной, `terms-2024-10-15` у архивной (ISO-дата: сортируется как текст, не зависит от локали) |
| `Document type` | Reference → `Legal document types` | группирует редакции одного документа; та же коллекция кормит табы |
| `Effective date` | DateTime | дата в шапке и в селекторе |
| `Status` | Option | `pointer` / `upcoming` / `in-force` / `archived` — см. «Статусы редакций». Заменяет прежний свитч `Is archived` (23.09.2026) |
| `Body` | RichText | тело, включая embed с таблицей |
| meta title / description / OG | — | сейчас забиты руками на каждой странице |

Вторая коллекция `Legal document types` — 5 записей (Terms, Privacy, Cookies, Affiliate, DPA),
нужна для reference-фильтра селектора и заодно кормит ряд табов, который сейчас статичный.

## Статусы редакций и запись-указатель (решено 23.09.2026)

Вводная от юристов 23.09.2026: 27 сентября вступают в силу обновлённые Terms, Privacy, Cookie
и DPA (Referral Program T&C не меняется). Клиентов уведомляют рассылкой, и между уведомлением
и вступлением в силу страницы должны **показывать действующую редакцию, но давать прочитать
будущую**. Прежняя схема «действующая по недатированному слагу, архивы по датированным» этого
не выдерживает: будущей редакции в ней нет места.

### Запись-указатель

Недатированная запись (`terms`) перестаёт быть носителем текста и становится **стабильным
адресом**: своего тела у неё нет, тело она рендерит из списка, отфильтрованного «этот же документ
+ статус *в силе*», лимит 1. Снаружи это обычная страница с полным текстом — ссылки в футере,
в письмах, в приложении и в подписанных договорах указывают на `/legal/terms` и не меняются никогда.

Почему так, а не «копировать тело в недатированную запись при смене редакции»:

- переключение статуса — единственное действие, и его делает юрист сам, без Designer и без агента;
- 62 КБ юридического текста никто не переносит руками — второй копии вообще не возникает;
- у каждой редакции постоянный датированный адрес **с рождения и навсегда**: ссылка из рассылки
  не протухает, и именно её можно вписать в договор и в запись о согласии.

Цена: в шаблоне появляется второй блок «дата + тело» под условием (на указателе — из списка,
на датированной странице — своё). Собирается один раз, дальше следить, чтобы два блока не разъехались.

Отвергнутый вариант — менять слаги местами при смене редакции. Тогда датированный адрес действующей
редакции живёт только пока она **не** в силе, между этими периодами нужен 301, который потом надо
не забыть снять. Забыли — архивный адрес молча отдаёт более новый текст. Для юр-документа недопустимо.

### Поле `Status`

`Is archived` (Switch) заменяется на `Status` (Option) с четырьмя взаимоисключающими значениями.
Именно одним полем, а не парой свитчей: два свитча можно случайно включить разом и получить
две «действующие» редакции одного документа.

| Значение | Пример слага | Где видно |
|---|---|---|
| `pointer` | `terms` | стабильный адрес, индексируется, тело из действующей редакции |
| `upcoming` | `terms-2026-09-27` | только по прямой ссылке, `noindex`, **в селекторе нет** |
| `in-force` | `terms-2024-10-15` | `noindex`, верхняя строка селектора |
| `archived` | `terms-2023-…` | `noindex`, селектор и блок «Previous versions» |

**Индексируются только пять стабильных адресов**, все датированные страницы `noindex` независимо
от статуса. Тогда не встаёт вопрос о дубликате между `/legal/terms` и датированным адресом
действующей редакции, пока они показывают один текст.

### Как отмечается действующая редакция

**Никак — она просто первая в списке.** Селектор сортируется по дате вниз и не показывает
`upcoming`, поэтому верхняя строка **всегда** действующая: это свойство сортировки, а не
соглашение, о котором надо догадываться. Ярлык дропдауна показывает ту же дату, что и верхняя
строка, — подтверждение приходит дважды без единого нового знака.

Решение от 22.09.2026 «точки в селекторе нет» этим сохраняется. Промежуточный вариант — вернуть
точку со смыслом «что сейчас в силе», раз на стабильном адресе «где я» и «что в силе» расходятся —
отклонён: дешевле убрать вопрос, чем добавить признак.

**Будущая редакция в селекторе не показывается.** Причина не в аккуратности списка: голая дата
в выпадающем списке не несёт контекста, и человек решит, что эти условия уже на него
распространяются. О будущей редакции сообщает плашка — в ней есть и дата, и слова «takes effect»,
и она заметнее строки в дропдауне, так что находимость не страдает.

Следствие: **на странице `upcoming` селектора нет вовсе** — её самой в списке нет, иначе вышел бы
ярлык с датой, которой в выпадающем списке не существует. Дата там становится обычным текстом без
стрелки — тот же режим, что уже собран для документа с единственной редакцией.

### Плашки

Все три собираются одним приёмом — **весь блок лежит внутри отфильтрованного Collection List**,
и когда подходящей записи нет, разметки нет вообще (тот же механизм, что у баннера архива):

| Где | Условие | Текст |
|---|---|---|
| стабильный адрес и действующая редакция | список: этот документ + `upcoming`, лимит 1 | «Updated terms take effect on September 27, 2026 — read them now» |
| страница `upcoming` | статус записи = `upcoming` | «This version takes effect on September 27, 2026» + ссылка на действующую |
| страница `archived` | статус записи = `archived` | «You're viewing an archived document. See the current version →» |

Плашка про будущую редакцию исчезает **сама** в момент смены статуса — отдельного шага «убрать
плашку» в процессе нет.

**Блока «Previous versions» внизу страницы не будет** (решено 23.09.2026). Он был во вводных
юристов, но те писались до того, как появился селектор редакций, а селектор закрывает то же
требование: в нём тот же список дат, теми же настоящими ссылками в разметке. Держать на одной
странице два одинаковых списка незачем.

Довод «аудитору удобнее список внизу» не выдержал проверки: аудитор ищет историю целенаправленно
и откроет дропдаун. Единственное, что терялось — находимость для того, кто историю **не** искал:
подпись «Effective date: June 8, 2026 ⌄» не объявляет себя как список версий. Это сочли
недостаточным основанием.

Автору вводных об этом надо сказать отдельно, чтобы пункт не выглядел потерянным: историю не
выбросили, она переехала в шапку, ближе к дате, которую человек и так читает.

Когда редакций станет с десяток, дропдаун перестанет быть годным контейнером — вот тогда блок
внизу и появится. Заранее не делаем.

## Выпуск новой редакции (целевой процесс)

Без Designer — может делать юрист сам:

1. **Заранее.** Завести запись `<doc>-<ГГГГ-ММ-ДД>` с новым телом, `Status: upcoming`, дата
   вступления в силу. Опубликовать. Адрес сразу рабочий — его и дают в рассылку. На стабильных
   адресах сама собой появляется плашка.
2. **В день вступления в силу.** У новой записи `Status: upcoming → in-force`, у прежней
   действующей `in-force → archived`. Опубликовать.

Стабильный адрес начинает отдавать новый текст, плашка исчезает, прежняя редакция уходит в селектор
и в «Previous versions». Ни один адрес не меняется, редиректы не нужны.

## Effective date наружу

Вопрос из вводных: можно ли вытащить дату программно — фронт передаёт её вместе с согласием
пользователя при регистрации.

Да. `effective-date` — настоящее поле CMS (DateTime), а не текст внутри тела. API отдаёт ровную
полночь UTC (`2024-10-15T00:00:00.000Z`), так что сдвига на сутки по таймзонам не будет.

Читать напрямую из браузера нельзя — Data API требует токен, а токену не место во фронте. Схема:
бэкенд ходит в Data API и кэширует.

**В запись о согласии класть не только дату, а слаг конкретной редакции** (`terms-2026-09-27`).
Тогда на вопрос «с чем именно согласился этот клиент» отвечает постоянная ссылка на замороженный
текст, а не реконструкция по дате. Вся схема датированных адресов ровно под это и строится.

## Что проверить до старта

1. **Префикс `legal` для коллекции.** Чтобы `/legal/terms` остался тем же адресом, коллекции нужен
   слаг `legal`, а папка `legal` с тем же именем существует. Скорее всего Webflow не даст, пока
   папка на месте. Порядок безопасной проверки — ниже, в «Порядке работ».
2. **Фильтр селектора по текущей записи.** Шаблон один на все документы, значит Collection List
   должен фильтроваться «редакции того же документа, что и открытая запись» — стандартный паттерн
   «похожие статьи» через reference-поле. В Designer он есть; **через MCP словарь фильтров
   (`{fieldSlug, operator, value}`) принимает только литеральные значения**, то есть настраивать
   фильтр, возможно, придётся руками в Designer. Фолбэк, если нативно не выйдет: рендерить все
   редакции и фильтровать по `data`-атрибуту скриптом.

## Порядок работ

Пункты 1–4 сделаны 22.09.2026. Остался катовер.

1. ~~Создать коллекции с временным слагом.~~
2. ~~Залить пять документов через API.~~
3. ~~Собрать шаблон: табы, заголовок, селектор, баннер, SEO, OG.~~
4. ~~Проверить на стейджинге по адресам `/legal-documents/*`.~~

### 5. Катовер — одной сессией, по порядку

Порядок важен: пока папка `legal` существует, коллекция не может забрать этот префикс.

| # | Шаг | Кто |
|---|---|---|
| 0 | ~~**Перепривязать табы** — пять ссылок в компоненте `legal-menu-tabs` с `linkType: page` на литеральные адреса~~ (23.09.2026) | агент (MCP) |
| 0б | **Перепривязать ссылки в футерах** — `Global / Footer Black` и `footer dark`, по три ссылки в каждом (Terms, Privacy, Cookies) | агент (MCP) |
| 1 | ~~`opt-out` выносится из папки: `parentFolderId: null`, слаг `opt-out`~~ (23.09.2026) | агент (MCP) |
| 4 | 301 с `/legal/opt-out` на `/opt-out` — **поднят перед удалением** (23.09.2026): между выносом страницы и редиректом `/legal/opt-out` отдаёт 404, если кто-то опубликует сайт | **человек**, Site settings |
| 2 | Удалить пять статичных юр-страниц и саму папку `legal` | **человек**, Designer |
| 3 | Слаг коллекции `legal-documents` → `legal` | **человек**, настройки коллекции |
| 5 | Убрать временный `<meta name="robots">` из head шаблона | агент (MCP) |
| 6 | Удалить тестовую запись `cookies-2024-10-15` | агент (API) |
| 7 | Публикация **на прод** | **человек**, с явного согласия |

**Шаг 0 обязателен и идёт строго первым.** Все пять табов привязаны по `pageId` к статичным
страницам, которые на шаге 2 удаляются: `…adfafe`, `…adfae2`, `…adfa58`, `…adfae4`, `…c2d290`.
Удалить страницы раньше, чем переписать ссылки, — значит сломать ряд табов **везде, где стоит
компонент**, включая живые юр-страницы прода. До смены слага литеральные адреса указывают ровно
туда же, куда указывали привязки, так что шаг визуально ничего не меняет и безопасен заранее.
Привязать таб к конкретной записи коллекции из статичного компонента нельзя — только литералом.

**Табами дело не ограничилось (23.09.2026).** Проверка остальных компонентов показала, что на те же
страницы ссылаются привязкой **оба футера** — `Global / Footer Black` и `footer dark`, по три ссылки
в каждом (Terms, Privacy, Cookies). Футер стоит на каждой странице сайта, так что удаление страниц
раньше перепривязки обнулило бы три ссылки в подвале по всему `bir.ch`. Отсюда правило:
**перед удалением любой страницы искать привязки к ней во всех компонентах, а не только в том,
который на виду.** Ссылка на `opt-out` из футера при этом трогать не нужно — привязка по `pageId`
следует за страницей и сама станет `/opt-out`.

Сплошная проверка компонентов (23.09.2026), `instanceCount` — сколько раз компонент стоит на сайте:

| Компонент | Инстансов | Ссылки на удаляемые страницы |
|---|---|---|
| `legal-menu-tabs` | — | 5 — перепривязаны |
| `Global / Footer Black` | **102** | 3 (Terms, Privacy, Cookies) — перепривязаны |
| `footer dark` | 0 | 3 — перепривязаны на всякий случай |
| `super-navigation-component` | — | нет |
| `Navigation` | 1 | нет |
| `Navigation White` | 4 | нет |
| `super-navigation-backup` | 0 | не проверялся, инстансов нет |
| `Global / Product Update Banner` | — | ссылок нет вообще |

**Что этой проверкой не закрыто:** отдельные страницы могут ссылаться на юр-документы привязкой
прямо в своём содержимом — например дисклеймер «by signing up you agree to our Terms» на странице
регистрации или демо. Компонентный обход таких ссылок не видит, а обходить все 123 страницы
поштучно дорого. Поэтому страховка процедурная: **после удаления страниц публикуем сначала на
стейджинг** и проверяем там живые адреса юр-документов, и только потом — прод. Битая ссылка
на странице после удаления цели становится пустой (`#`), так что на стейджинге она видна.

Почему часть шагов только руками: в MCP нет ни `delete_page`, ни смены слага коллекции, ни
управления редиректами.

**Публикация на прод — отдельное решение.** Полная публикация выкладывает и чужие неопубликованные
правки в Designer, поэтому спрашивать каждый раз (правило в памяти агента). До катовера все
публикации шли только на стейджинг: `publishToWebflowSubdomain: true` с пустым `customDomains`.

### Катовер выполнен 23.09.2026 — что получилось

Порядок отличался от записанного: **страницы не удалялись.** Папка `legal` переименована в
`legal-old` (решение дизайнера) — префикс освобождается так же, но старые страницы остаются
на месте как запасной вариант. Чтобы они при этом не стали пятью новыми публичными адресами
с копиями юр-документов, все пять переведены в `draft` и из публикации исключены.

Это лучше удаления: откат — один переключатель, а не восстановление страниц из небытия.
Удалить их можно позже, когда CMS-версия отработает без нареканий.

| Шаг | Статус |
|---|---|
| 0 Табы на литеральные адреса | сделано, агент |
| 0б Футеры на литеральные адреса | сделано, агент |
| 1 `opt-out` из папки → `/opt-out` | сделано, агент |
| 4 301 с `/legal/opt-out` | сделано, дизайнер |
| 2 Папка `legal` → `legal-old`, пять страниц в `draft` | сделано, дизайнер + агент |
| 3 Слаг коллекции → `legal` | сделано, дизайнер |
| 5 Временный `robots` из шаблона убран | сделано, агент |
| 6 Удалить тестовую запись `cookies-2024-10-15` | **не сделано** — держим, пока не проверена глазами |
| 7 Публикация на прод | **не сделано** — ждёт решения |

**Проверено на стейджинге** (`birch-testing.webflow.io`, публикация 23.09.2026):

- пять адресов `/legal/*` отдают 200, `/legal-old/*` — 404, `/legal/opt-out` → 301 → `/opt-out`;
- табы вышли литеральными, и подсветка активного (`w--current` + `aria-current="page"`)
  **работает** — она сравнивает адрес ссылки с адресом страницы, литералу это не мешает;
- в футере `/legal/terms` тоже получает `w--current`;
- `meta description`, OG и `canonical` (`https://bir.ch/legal/terms`) подтянулись из полей;
- у действующей редакции баннера архива и скрипта `noindex` в разметке **нет вообще** —
  условная видимость вырезает их целиком, как и задумано;
- у архивной — баннер, ссылка на действующую, обе редакции в селекторе, открытая помечена
  `w--current`, и скрипт, дописывающий `robots: noindex, follow` в `head`;
- **текст сверен:** видимый текст `/legal/terms` на стейджинге совпал с полем `body` в CMS
  посимвольно — 23 004 знака, то же число, что дала сверка при переносе 22.09.

Свежеопубликованная CMS-запись отвечает **не сразу**: `/legal/cookies-2024-10-15` отдавал 404
через несколько секунд после публикации и 200 через минуту. Проверять адреса сразу после
публикации и делать вывод по первому ответу нельзя.

### После катовера — проверить

- Пять адресов `/legal/<slug>` открываются и отдают тот же текст, что до переноса.
- `/legal/opt-out` редиректит на `/opt-out`.
- Табы в шапке ведут на CMS-страницы, а не на 404, и подсветка активного таба (`w--current`)
  по-прежнему срабатывает — она сравнивает адрес ссылки с адресом страницы, литералу это не мешает.
- У действующих редакций в head **нет** `robots`, у архивных есть `noindex, follow`.
- Селектор у документа с одной редакцией — обычный текст без стрелки.

### 6. Перевод на статусы — сразу после катовера

Катовер переносит `/legal/*` на CMS в нынешнем виде (недатированная запись = сам документ).
Схема со статусами накладывается следующим шагом:

| # | Шаг | Кто |
|---|---|---|
| 1 | Завести поле `Status` (Option, 4 значения), удалить `Is archived` | агент (API) |
| 2 | Из каждой из пяти записей сделать датированный дубль `<doc>-<дата>` со статусом `in-force`, исходную перевести в `pointer` и очистить у неё тело | агент (API) |
| 3 | Шаблон: второй блок «дата + тело» под условием, плашка про будущую редакцию, блок «Previous versions», селектор прячется на `upcoming` | агент (MCP) |
| 4 | Фильтры списков и условная видимость по `Status` | **человек**, Designer |
| 5 | Проверка на стейджинге на паре `terms` / `terms-<дата>` | агент |

### 7. Релиз 27 сентября

| Когда | Шаг | Кто |
|---|---|---|
| как придут тексты | завести четыре записи `upcoming` с новыми телами и датой 2026-09-27, опубликовать | агент (API) |
| перед рассылкой | отдать четыре прямых адреса для письма клиентам | агент |
| 27.09.2026 | по два переключения статуса на документ | юрист |

**Сроки.** Уведомление планировалось на 18.09.2026, к 23.09.2026 рассылка не ушла. Если интервал
между уведомлением и вступлением в силу задан самим договором («we will notify you at least N days
in advance»), то при отправке письма после 18-го он может быть уже не выдержан, и двигать надо не
рассылку, а дату вступления в силу. Проверить это может только тот, кто знает текст действующих
Terms; вынесено автору вводных 23.09.2026.

### Правки после первого просмотра (23.09.2026)

**Плашка архива сидела несимметрично** — 16px сверху, 24px снизу при ровном паддинге 16.
Причина не в паддинге: у `.legal-archive-note` стоит `flex-direction: column` и
`grid-row-gap: 0.5rem`, а вторым флекс-элементом в ней лежит **embed со скриптом `noindex`**.
Он нулевой высоты и невидим, но промежуток перед ним браузер всё равно откладывает. Лечится
обнулением `grid-row-gap` — видимый ребёнок в плашке один, промежутку не между чем стоять.

Общее правило: **невидимый элемент остаётся флекс-элементом.** Скрипт, пустой div, embed без
вывода — всё это участвует в раскладке и получает свою долю `gap`. Если в контейнере с `gap`
лежит служебный элемент, симметрия ломается ровно на величину промежутка, и снаружи это читается
как неверный паддинг.

**Таб не подсвечивался на датированных редакциях.** Webflow ставит `w--current` только при
точном совпадении адреса ссылки с адресом страницы, поэтому на `/legal/cookies-2024-10-15`
не горел ни один таб и ряд выглядел сломанным. Нативного способа подсветить «по группе» нет.
Решено скриптом в подвале шаблона, каноника — [`webflow/legal-template.footer.html`](../webflow/legal-template.footer.html):
таб считается текущим, если путь равен его `href` или начинается с `href + "-"`.

Префикс берётся из самого `href`, не прописывается списком — переименование слага документа
ничего здесь не ломает. Разделитель именно `-`: совпадение по голому префиксу подсвечивало бы
и соседа, чей слаг начинается с тех же букв.

Переиспользовать сайтовый механизм `data-match-prefix` не вышло: обработчик живёт **внутри
компонента навигации** и смотрит только на свои ссылки; ни в `<head>`, ни в подвале сайта его нет.

Скрипт обязан идти через `Webflow.push`, а не по `DOMContentLoaded`: модуль ссылок Webflow на
`ready` снимает `w--current` со всех ссылок, чей адрес не совпал точно, и стирает чужую отметку.
Первая версия из-за этого выглядела неработающей, хотя тот же код из консоли отрабатывал. Разбор —
в `webflow-mcp.md`.

### Плашка стала общей: архив + будущая редакция (23.09.2026)

Классы переименованы из `legal-archive-*` в нейтральные `legal-note-*`: одна и та же плашка
обслуживает оба случая, поэтому «archive» в имени стало неверным. Внутри неё два текста под
условиями и **один общий список** со ссылкой на действующую редакцию — он одинаково нужен обоим.

```
.legal-note                      ← видна, когда Status ≠ In force И ≠ Stable address
  .legal-note-line
    p.legal-note-txt             «You're viewing an archived document.»   ← Status = Archived
    .legal-note-upcoming         ← Status = Upcoming
      p.legal-note-txt           «This version takes effect on»
      div.legal-note-date        ← привязан к полю Effective date
      p.legal-note-txt           «.»
    CollectionList → a           «See the current version →»
```

Почему одна плашка, а не две: список со ссылкой настраивается только руками в Designer
(фильтр по текущей записи), и две плашки означали бы два таких списка. Условие «показать, когда
Status ни то, ни это» выражается двумя правилами через И — отрицание в условиях Webflow есть,
дизъюнкции нет, поэтому условие сформулировано через два «не».

**Дата подставляется автоматически** — элемент привязан к полю `Effective date` записи, а не
набирается текстом. Руками дату никто не вписывает, разойтись с полем она не может.

Точка вынесена отдельным элементом, потому что у строки `gap: 0.25em`: будь точка частью общего
потока с промежутками, она отъехала бы от даты. У `.legal-note-upcoming` поэтому `gap: 0`, а
отступ перед датой задан её собственным `margin-left: 0.25em`.

**Условная видимость по Option-полю через API не ставится.** Проверено напрямую: `set_visibility`
с привязкой к `Status` отвечает `Incompatible binding: CMS field cannot bind to target`. Свитч
привязывается, Option — нет. Значит условия по `Status` — операция Designer, как и фильтры списков.

### Что настроить в Designer (23.09.2026)

| Элемент в Navigator | Что задать |
|---|---|
| `Note (archived + upcoming)` | видимость: Status ≠ `In force` И Status ≠ `Stable address` |
| `Note — ARCHIVED text` | видимость: Status = `Archived` |
| `Note — UPCOMING text` | видимость: Status = `Upcoming` |
| `Effective date + version selector` | видимость: Status ≠ `Upcoming` |
| `List — link to version IN FORCE` | фильтр: тип документа = текущий **и Status = `In force`** |
| Список редакций в селекторе | фильтр: добавить Status ≠ `Upcoming` |

**Условия поставлены и проверены 23.09.2026.** В UI Webflow условие `is not any` принимает
**список** значений, поэтому VIS-1 умещается в одно правило, а не в два через AND, как
предполагалось выше.

Проверка трёх состояний на стейджинге (`/legal/cookies`, `cookies-2024-10-15`, `cookies-2026-09-27`):

| | действующая | архивная | будущая |
|---|---|---|---|
| плашка | нет | «archived» | «takes effect on 27 September 2026» |
| ссылка из плашки | — | `/legal/cookies` | `/legal/cookies` |
| блок даты и селектор | есть | есть | **нет** |
| варианты в селекторе | 8 июня, 15 октября | те же | — |
| `noindex` | нет | есть | есть |
| подсветка таба | есть | есть | есть |

27 сентября из селектора действующей редакции исчезло, ссылка из архивной плашки ведёт на
действующую, а не на будущую. Дата в плашке подставилась из поля в том же формате, что в селекторе;
точка стоит вплотную (замер: промежуток между датой и точкой 0px, перед датой 3,5px = 0,25em).

**Незакрытый случай.** Скрипт `noindex` лежит внутри плашки, а плашка не показывается на
действующей редакции. Пока действующие редакции живут по недатированным адресам, это верно. Но
в модели с записью-указателем появится **датированная** редакция со статусом `In force` — у неё
плашки не будет, а значит не будет и `noindex`, хотя правило гласит «индексируются только пять
стабильных адресов». Решить при переходе на указатели; пометка оставлена прямо в коде embed'а.

**Строка про `FILTER-1` — это баг, который вносит появление будущей редакции.** Список ссылки «See the
current version» сейчас настроен как «сортировка по дате вниз, лимит 1», то есть «самая свежая».
Пока будущих редакций не было, самая свежая и была действующей. Теперь самая свежая — это
редакция от 27 сентября, которая ещё не вступила в силу, и архивная плашка повела бы на неё.
Признак «действует» должен быть явным, а не выводиться из даты.

### Переход на записи-указатели (начат 23.09.2026)

Данные переведены: у каждого из пяти документов заведена датированная запись со статусом
`In force`, куда уехал текст, а недатированная получила статус `Stable address`.

| Указатель | Датированная редакция | Знаков |
|---|---|---|
| `terms` | `terms-2024-10-15` | 24 428 |
| `privacy` | `privacy-2026-06-08` | 32 467 |
| `cookies` | `cookies-2026-06-08` | 9 922 |
| `data-processing` | `data-processing-2026-06-08` | 61 101 |
| `referral-program-terms-and-conditions` | `referral-program-terms-and-conditions-2024-10-15` | 7 576 |

Поле `Body` стало необязательным — у указателя текста нет по замыслу.

**Переход завершён 23.09.2026.** Условия видимости расставлены, списки подключены, тела
у указателей очищены. Проверено на стейджинге: все пять адресов `/legal/*` показывают текст
действующей редакции и её дату, имея пустое поле `Body`; на странице ровно один блок текста —
дублирования нет. `noindex` стоит у всех датированных адресов и отсутствует у пяти стабильных.

Ниже — почему тела временно возвращались по ходу работы (оставлено как разбор случая).

**Тело у указателей временно оставлялось на месте.** Очистка тел сделала бы юр-страницы пустыми
до того, как шаблон научится брать текст у действующей редакции, а записи CMS уже опубликованы —
любая публикация на прод кем угодно из команды выложила бы пустые Terms. Тела вернули; удалить их
можно будет после того, как заработает условие, скрывающее это поле на указателе. Лишнее значение
в скрытом поле безвредно, пустая юридическая страница — нет.

**Два новых ограничения API (23.09.2026):**

1. **Источник коллекции у Collection List через API не задаётся.** В настройках элемента есть
   только `domId`, `tag` и `visibility` — поля «какая коллекция» нет вовсе.
2. **В неподключённый список нельзя добавлять элементы.** Билдер отвечает
   `Connect this Collection List to a Collection before adding elements`.

Отсюда порядок работ жёсткий: **человек подключает список в Designer → агент кладёт внутрь
привязанные элементы → человек ставит фильтры и условия.** Одним заходом агента не закрывается.

### Что настроить в Designer, шаг 2 (23.09.2026)

| Слой | Что сделать |
|---|---|
| `LIST-3` (в секции тела, сразу после Rich Text) | подключить к `Legal documents`; фильтр: тип документа = текущий **и** `Status` = `In force`; лимит 1 |
| `LIST-4` (внутри переключателя даты) | то же самое |
| `FILTER-2` (список редакций в селекторе) | добавить `Stable address` в исключения: `is not any [Upcoming, Stable address]` |

Третья строка обязательна: без неё сама запись-указатель попадёт в селектор отдельной строкой.

После подключения списков агент кладёт внутрь Rich Text (привязка к `Body`) и текст даты
(привязка к `Effective date`), и тогда ставятся последние условия видимости:

| Слой | Условие |
|---|---|
| существующий Rich Text с телом | `Status` ≠ `Stable address` |
| `LIST-3` | `Status` = `Stable address` |
| существующий элемент даты в переключателе | `Status` ≠ `Stable address` |
| `LIST-4` | `Status` = `Stable address` |

### `noindex` переехал на признак адреса (23.09.2026)

Схема «скрипт лежит внутри плашки, плашка показывается только у недействующих редакций» рухнула
ровно там, где и было предсказано: с появлением указателей возникла **датированная редакция
со статусом `In force`**. Плашки у неё нет, значит не было и `noindex` — и `/legal/cookies-2026-06-08`
стала второй индексируемой копией действующей Cookie Policy.

Две попытки привязать правило к полю `Status` не прошли:

1. **Атрибут, привязанный к полю, через API не ставится** — `set_settings` с `value_binding`
   отвечает `attributes[0]: value must be a string or a binding`. Раньше так же отказал Switch,
   теперь и Option: **атрибуты к полям CMS через API не привязываются вообще.**
2. Условная видимость по `Status` — только Designer (см. выше), то есть правило снова зависело бы
   от настройки, которую надо помнить при пересборке.

Рабочее решение — **признак берётся из адреса**: слаг редакции всегда `<документ>-ГГГГ-ММ-ДД`,
у стабильного адреса такого хвоста нет никогда. Страница определяет себя сама, без привязок и без
настроек в Designer:

```js
if (!/-\d{4}-\d{2}-\d{2}$/.test(location.pathname.replace(/\/+$/, ''))) return;
```

Правило живёт в подвале шаблона рядом со скриптом подсветки таба, каноника —
[`webflow/legal-template.footer.html`](../webflow/legal-template.footer.html). Старый embed внутри
плашки удалён, иначе метатег дописывался бы дважды.

Побочная польза: правило теперь читается ровно так, как было сформулировано изначально —
«индексируются только пять стабильных адресов», — а не выводится из того, какая плашка показана.

### Импорт четырёх новых редакций (23.09.2026)

Источники — четыре `.docx` от юристов. Имя файлов обещает редлайн, но **правок в них нет ни одной**
(ноль вставок, удалений и комментариев во всех четырёх): это чистовики. Стоит переспросить, точно
ли это финальные версии.

**Дата — 28 сентября 2026**, не та, что внутри файлов. В файлах: Terms, Cookie и DPA говорят
22.09.2026, Privacy несёт незаполненный плейсхолдер `[DATE OF PUBLICATION]`.

**Залиты со статусом `Upcoming`, не `In force`.** Сегодня 23-е, вступают в силу 28-го; поставь им
`In force` сразу — указатели начали бы отдавать новый текст на пять дней раньше срока. `Upcoming`
означает «лежит по своему адресу, в селекторе нет, из поиска закрыта»: юристы могут посмотреть
готовые страницы, снаружи ничего не меняется. 28-го — два переключения статуса на документ.

Конвертер: [`webflow/docx-to-richtext.py`](../webflow/docx-to-richtext.py). Написан намеренно
узким — понимает ровно то, что есть в этих файлах, и падает на незнакомом вместо того, чтобы
молча пропустить. Внутренних якорей нет ни в одном документе (проверено), так что переезд в
rich text безопасен, как и в первый раз.

Правила преобразования:

- заголовок документа (`Heading1`) и строка `Effective date` из тела **убираются** — и то и другое
  страница уже показывает из полей;
- уровни заголовков приводятся к общей шкале: у Terms `Heading3` → `h2`, у Privacy `Heading3` → `h2`
  и `Heading6` → `h3`, у Cookie и DPA `Heading2` → `h2`;
- пустые абзацы и пустые заголовки выбрасываются;
- списки только маркированные (проверено по `numbering.xml`) → `<ul>`;
- таблицы заворачиваются в `<div data-rt-embed-type='true'><div class="rt-table">`, ширины колонок
  считаются из `tblGrid` в проценты. У DPA сетка колонок в файле продублирована (шесть значений на
  три колонки) — лишнее отбрасывается.

**Сверка:** видимый текст исходника сравнён с видимым текстом результата без учёта пробелов —
совпало посимвольно у всех четырёх (29 715 / 27 244 / 7 364 / 51 055 знаков). Сравнение с учётом
пробелов даёт ложное расхождение: разбор вставляет пробел на границе блоков, в docx его нет.
Затем тела выкачаны обратно из CMS и сверены с отправленным — совпали байт в байт.

### Дефекты в исходниках — передать юристам

1. **В Terms отсутствует раздел 4.** Нумерация идёт 1, 2, 3, 5, 6… Раздела с номером 4 нет
   нигде — ни заголовком, ни абзацем. Похоже на удалённый раздел без перенумерации.
2. **Два раздела потеряли стиль заголовка:** «12. Operation of service» в Terms и
   «6. Security Measures» в DPA размечены обычным текстом, хотя их соседи — заголовки. При импорте
   уровень заголовка возвращён: намерение однозначно, текст не менялся.
3. **В DPA абзац на 643 знака размечен заголовком** — «Controller and Processor: For purposes of
   GDPR…». Это тело раздела 1. При импорте сделан обычным абзацем.
4. **Заголовок в Terms — «Terms of Use»**, тогда как страница и таб называются
   «Terms and Conditions». Сам заголовок из тела убран, но имя стоит сверить.

### Таблицы шире колонки текста (23.09.2026)

Колонка рассчитана на прозу (~75 знаков в строке), и справочная таблица в ней складывается
в пять-шесть строк на ячейку. Таблицы выведены за колонку, оставаясь по центру.

Замеры на таблице суб-процессоров DPA (12 строк):

| Ширина | Высота таблицы | Средняя строка |
|---|---|---|
| 555px (колонка) | 1243px | 100px |
| 704px | 1071px | 86px |
| **864px (принято)** | **935px** | **75px** |
| 1024px | 781px | 62px |

Взято `min(54rem, calc(100vw - 3rem))`. Центрирование через `margin-left: 50%` +
`transform: translateX(-50%)`, а не отрицательными маргинами: ширина тогда задаётся одним числом,
и таблица остаётся по центру независимо от того, что происходит с колонкой. `min()` с вьюпортом
не даёт ей вылезти за экран на узких окнах — там она по-прежнему скроллится вбок.

### Линии в таблице: дело было в цвете, не в толщине (23.09.2026)

Разделители строк не читались. Первая догадка — тонкая линия, надо удвоить. Замер показал другое:
линия **1px**, а цвет — `light-gray` `#edecf2`, контраст на белом **1,17:1**. Это не «тонко»,
это «почти белым по белому».

Удвоение толщины блёклого цвета даёт не линию, а светло-серую полосу — читается как грязь, а не
как разделитель. Поэтому сначала цвет: взят токен **`hairline`** (`#0d0d0d1f`, чёрный на 12%),
заведённый в палитре ровно под разделители; толщина осталась 1px.

| Токен | Цвет | Контраст на белом |
|---|---|---|
| `light-gray` (было) | `#edecf2` | 1,17 |
| **`hairline` (стало)** | `#0d0d0d1f` | **1,28** |
| `muted-gray` | `#dbdae2` | 1,39 |
| `mid-gray` | `#909090` | 3,19 |

**Значение захардкожено осознанно (24.09.2026).** `hairline` дал 1,3 — на таблице шириной 1024px
и кеглем как у тела этого не хватило: глазу нечем вести строку. Между 1,39 и 3,19 в палитре пусто,
а разделителю плотной таблицы нужно ровно там, поэтому взято литеральное **`#c4c3ce`** — контраст
**1,74:1**, холодный оттенок под палитру (её серые уходят в синеву).

Толщину не трогали: 2px блёклого цвета дают полосу, а не линию.

**Токена под разделитель таблицы в системе нет**, и правильный ход — завести его через
`/figma-sync`, чтобы Figma и код сошлись. До тех пор литерал честнее, чем подбор ближайшего по
списку: он прямо говорит, что значение выбрано вне системы, и заменяется одной строкой. Причина
и все замеры записаны в комментарии рядом с ним, чтобы следующий человек не гадал.

**Правило на будущее:** прежде чем менять толщину линии, померить её контраст с фоном. Невидимая
линия почти всегда слишком светлая, а не слишком тонкая, и толщина лечит симптом.

### Свитч `Is archived` удалён (23.09.2026)

Перед удалением проверено опытом, а не рассуждением: у единственной записи со включённым свитчем
он был выключен, и плашка архива осталась на месте — значит держится только на `Status`. После
этого поле удалено. Проверять было нужно, потому что условия видимости через API не читаются:
рассуждение «мы всё перевели на Status» доказательством не является.

### Потерянные переводы строки — и почему сверка их пропустила (23.09.2026)

Первая версия конвертера **выбрасывала `<w:br/>`** — переводы строки внутри абзаца. Их 48:
35 в DPA, 8 в Terms, 5 в Privacy. Буквы при этом все сохранялись, а структура местами склеивалась:
в Terms заголовок «4. Disclaimer and limitation of liability» приклеился к концу предыдущего
абзаца («…TikTok Ads.4. Disclaimer…»). Из-за этого раздел 4 выглядел отсутствующим — его искали
как абзац, начинающийся с «4.», а такого в документе нет.

**Сверка это пропустила по построению.** Я сравнивал текст, предварительно убрав все пробелы, —
так было сделано, чтобы обойти ложное расхождение (разбор HTML вставляет пробел на границе
блоков, в docx его нет). Вместе с шумом отключился и сигнал: потеря переноса — это ровно потеря
пробельного символа.

**Новая сверка — построчная** ([`webflow/docx-verify.py`](../webflow/docx-verify.py)): каждый
абзац, ячейка и `<w:br/>` дают строку, списки строк сравниваются поэлементно. Совпало у всех
четырёх: 114 / 128 / 110 / 148 строк. Эта проверка поймала бы исходную ошибку.

**Правило:** нормализация, которая гасит ложное расхождение, гасит и настоящее того же рода.
Если пришлось что-то игнорировать при сверке — проверить отдельно именно это.

Переводы строки в документах значат разное, поэтому обрабатываются по контексту:

| Где | Сколько | Что делает конвертер |
|---|---|---|
| DPA, внутри пунктов списка | 35 | `<br>` внутри `<li>` — перенос после жирной подписи |
| Privacy, адреса и контакты | 3 | `<br>` внутри абзаца — строки адреса |
| Terms, между блоками | 4 | сегмент, похожий на нумерованный заголовок, выносится в `<h2>` |
| Privacy, внутри предложения | 1 | `<br>`; **в источнике пропал текст ссылки** на Google API Services User Data Policy |

Правило выноса в заголовок: сегмент короче 90 знаков и начинается с «N. ». Этим же правилом
подхватились «12. Operation of service» и «6. Security Measures», которые потеряли стиль заголовка.

### Оговорка о вступлении в силу (решено 24.09.2026)

По Section 13 изменение Terms действует для существующих аккаунтов через 30 дней после уведомления
по email; новые клиенты принимают текущую редакцию при регистрации. **Юристы подтвердили: правило
есть только в Terms и DPA, Privacy и Cookie вступают в силу сразу.**

Сами документы с 23.09.2026 несут эту оговорку первой строкой после даты. Поэтому выводить её
берёмся не пересказом, а как есть.

**Путь, который прошли и почему бросили:**

1. **Собранная фраза из двух дат** («Applies to accounts created on or after … and to all other
   accounts from …») с отдельным полем `Effective date — existing accounts`. Отброшено: документы
   теперь говорят это сами и точнее — в их формулировке есть оговорки, которых наша фраза не
   выражает («или с момента, когда вы приняли эту версию, если это раньше»). Поле и фраза удалены.
2. **Отдельное поле `Applicability note`** с текстом юристов, выводимое под селектором. Отброшено:
   добавляет юристу ручной шаг на каждом выпуске — вынуть абзац из своего документа и положить
   в поле. Забудет — под селектором пусто или текст на странице дважды. Плюс тело перестаёт быть
   полным документом, а именно его читает фронт для записи о согласии.
3. **Стиль по позиции** (`первый абзац тела`). Отброшено: позиция не равна смыслу. Добавят строку
   выше — серым станет не то.
4. **Принято: абзац остаётся в теле и размечается `Heading 6`.**

**Почему h6.** Это единственный тип блока, который редактор CMS может выбрать сам — произвольный
класс в rich text Webflow нативно не применяется ([Rich text element overview](https://help.webflow.com/hc/en-us/articles/33961256808467-Rich-text-element-overview)).
А шестой уровень в юр-документах не занят ничем настоящим: разделы размечаются h2, подразделы h3.
Соглашение записано **в подсказку поля `Body` в CMS**, где его видит юрист, а не только здесь.

Три свойства, ради которых это выбрано:

- **ничего не надо помнить** — разметка ставится в том же окне, где пишется текст;
- **документ остаётся цельным** — тело и есть документ, ничего не вынесено;
- **до стабильного адреса доезжает само** — тело указателя приходит из действующей редакции
  существующим списком, отдельный список и условия не нужны. Задача «приписка на стабильном
  адресе», висевшая с 23.09, этим закрылась и снята.

**Цена — семантика.** `h6` это заголовок, и предложение на 250 знаков в структуре документа
выглядит как заголовок шестого уровня среди h2 и h3, с пропуском уровней. Поэтому подвал шаблона
переписывает тег в обычный абзац с тем же стилем. Откажет скрипт — текст останется видимым и серым,
пострадает только структура: отказ идёт в безопасную сторону.

**Остаточный риск:** если юристам понадобится настоящий заголовок шестого уровня, он выйдет серым.
Документы до шестого уровня не доходят, но соглашение на это и записано в CMS.

Стиль живёт в кастом-коде шаблона (`.rt-legal h6`), потому что класс нельзя повесить на потомка
привязанного rich text — тот самый случай, который в CLAUDE.md записан как законное исключение.
Через API нестандартные тег-стили внутри класса не создаются: имена `p` и `h6` зарезервированы,
а каноническое имя («Paragraph») даёт комбо-класс, а не селектор потомка.

### Служебный текст был жирнее документа (23.09.2026)

Замер начертаний в шапке юр-страницы:

| Элемент | Вес |
|---|---|
| подпись «Effective date:» | 600 |
| дата в селекторе | 600 |
| приписка о датах | 600 |
| текст плашки | 600 |
| **тело документа** | **500** |

То есть вся обвязка была **жирнее самого юридического текста** — иерархия перевёрнута: главное
на странице набрано легче, чем подписи вокруг него. Вес приезжал из `paragraph-sm`, а не был
задан осознанно; мой `font-weight: 600` у дат в приписке вообще ничего не добавлял, там и так
было 600.

**Стало:** приписка и плашка — 500, как тело. На 600 осталась строка «Effective date: <дата>»
целиком: подпись и дата в селекторе — одна фраза, и разный вес рвал её надвое (цвет у них уже
разный, подпись серая, дата чёрная, так что вес — единственное, что их связывает).

Итоговая шкала: строка даты 600, всё остальное служебное 500, тело документа 500.

Даты внутри приписки специально **не** выделены. Приписка — одно цельное утверждение, и выделение
дат внутри дробит фразу на куски; читатель начинает собирать её из акцентов вместо того, чтобы
прочесть подряд.

**Выключка приписки — левая, по колонке документа** (решено 23.09.2026). Шапка центрована, но
приписка выровнена по левому краю и лежит в том же боксе `34.66rem`, что серая плашка и тело
документа: левые края всех трёх совпадают. Центрованная приписка в две строки читалась как
отдельный заголовок, а не как сноска к документу.

**Правило:** вес шрифта у служебных подписей проверять относительно **основного текста страницы**,
а не подбирать сам по себе. Если подпись тяжелее содержимого, которое она подписывает, — иерархия
перевёрнута, каким бы аккуратным ни выглядел сам по себе каждый элемент.

### Вертикальный ритм шапки (23.09.2026)

Замеры на 1280 (корень 16px), после правки:

| Промежуток | Было | Стало | Откуда |
|---|---|---|---|
| табы → заголовок | 48 | 48 | `legal-head-group` padding-top, токен `--_sizes---3rem` |
| строка даты → приписка | 12 | 48 | тот же `legal-head-group` padding-bottom |
| приписка → плашка | 48 | 20 | `legal-applies` margin-bottom, токен `--_sizes---1-25rem` |
| плашка → текст | 24 | 20 | `legal-note` margin-bottom, тот же токен |

**Приписка перенесена из шапки в секцию с текстом**, к плашке. Это не косметика: раньше два
промежутка вокруг заголовочного блока были **двумя независимыми числами**, и держать их равными
пришлось бы вручную — поправил одно, забыл второе. После переноса это верхний и нижний паддинг
**одной группы**, симметричные по определению; разойтись они не могут, даже если значение потом
поменяют.

По смыслу тоже верно: приписка и плашка обе говорят «прежде чем читать, учти вот что», а строка
даты — часть заголовка.

**Все значения на токенах.** Под «чуть меньше 24» в палитре ровно одно значение — `1.25rem` = 20px,
подбирать не пришлось. Паддинг группы тоже переведён с литерального `3rem` на переменную.

**Правило:** если два отступа должны быть равны, надёжнее сделать их **одним значением** (паддинг
одного контейнера с двух сторон), чем двумя равными. Равенство, которое держится на дисциплине,
рано или поздно разъезжается.

## Состояние на 24.09.2026 и что осталось

**Прод не тронут.** На живых адресах статика; коллекция сидит на слаге `legal-cms`, закрыта
временным `robots`, папка `legal` держит префикс. Откат сделан намеренно 23.09 и держится.

**Готово и проверено на стейджинге:**

- пять документов на записях-указателях; стабильный адрес отдаёт текст и дату действующей редакции;
- четыре новые редакции от 23.09 залиты со статусом `Upcoming` на 28 сентября, сверены с
  исходниками построчно;
- оговорка о вступлении в силу остаётся в теле и размечается `Heading 6`;
- у будущих редакций дата показана обычным текстом, без выбора;
- голые адреса в текстах стали ссылками — и в новых редакциях, и в старых;
- таблицы: ширина 64rem, кегль как у тела, разделитель `#c4c3ce`;
- с заголовков снята импортированная жирность.

**Осталось:**

| Когда | Что | Кто |
|---|---|---|
| перед публикацией | перечитать кастом-код шаблона (`<head>` и подвал) и сверить с `webflow/*.html` | агент |
| по решению | публикация на прод | **человек** |
| при возврате катовера | слаг коллекции → `legal`, папка → `legal-old`, пять страниц в `draft`, снять временный `robots` | человек + агент |
| 28 сентября | четыре записи `Upcoming → In force`, четыре прежние `In force → Archived` | юрист |
| 28 сентября | переименовать указатель Terms и подпись таба в «Terms of Use» | агент |

**Юристам передать:** правило 30 дней прописано в Terms и DPA, в Privacy и Cookie его нет —
подтверждено, что так и задумано. Незакрытого за ними не осталось.

**Тестовые записи:** `cookies-2024-10-15` (архивная) оставлена намеренно — на ней единственной
видно поведение архивной плашки. Тестовая будущая `cookies-2026-09-27` удалена 24.09.

### Affiliate: разделы не были заголовками (24.09.2026)

Сплошной прогон пятнадцати страниц показал у Affiliate **один заголовок на 6 780 знаков**, тогда
как у соседей 8–18. Этот документ единственный приехал не из docx, а из старой статичной страницы,
и там все восемнадцать разделов были размечены обычными абзацами: `<p><strong>1. Agreement
Terms</strong></p>`.

Подняты в `h2` тем же правилом, что применялось к импорту из Word, с той же проверкой: видимый
текст до и после совпал.

**Чем это плохо было** — не только внешним видом. Документ без заголовков не имеет оглавления:
по нему нельзя перемещаться с клавиатуры и скринридером, он не даёт якорей поиску и выглядит
сплошной простынёй на 38 абзацев.

**Открытый вопрос — у документа три разных названия:**

| Где | Как называется |
|---|---|
| таб в шапке | Affiliate Program |
| запись в CMS и `h1` страницы | Referral Program Terms and Conditions |
| первая строка тела | Affiliate Program Agreement |

Название в теле оставлено как есть: у остальных документов заголовок из тела убирался, потому что
он дословно повторял заголовок страницы, а здесь это **третья** формулировка, и выбрасывать её —
решение о содержании, а не об оформлении. Нужен ответ, как документ называется на самом деле.

**Правило:** сплошной прогон всех страниц с одинаковыми метриками находит то, чего не видно при
проверке по одной. Сравнивать соседей между собой — число заголовков, длину, число ссылок:
выбивающаяся строка почти всегда означает дефект, а не особенность документа.

## Типографика тела

**Сейчас у юр-страниц она сломана** — замеры на 1440px, `html { font-size: 17.6px }`:

| | колонка | `p` | line-height | вес `p` | заголовки |
|---|---|---|---|---|---|
| `.legal-rt` (юр.) | 800px | 15,84px | 19,0 (**1,2**) | **600** | h3 30/39, h5 19,8, **h6 12px** |
| `.cs-rich` (кейсы) | 763px | 17,6px | 26,4 (1,5) | 600 | h2 41,8, h3 26,4 |
| `.blogpost-rich-text` (блог) | — | 22px | 30,8 (1,4) | 500 | h2 44, h3 28,8 |

Класс `.legal-rt` почти пустой — в нём только `max-width: 800px` и правило для `h3`. Всё остальное
падает на **глобальные тег-стили**, и оттуда приезжает:

- **интерлиньяж 1,2 на теле** — для документа в 30 000 знаков это нечитаемо тесно;
- **вес 600 на всём теле** — полужирный от первой буквы до последней, акцент перестаёт работать;
- **`h6` (12px) мельче тела (15,84px)** — иерархия перевёрнута, а в Privacy таких заголовков 19;
- **строка ~100 знаков** (800px при 15,84px) при комфортных 60–75.

Донор значений — **`.birchcourse-rich-text`** (курс, `/birch-course/*`, есть свой styleguide:
`/styleguides/birch-course-styleguide`). Это самый полно прописанный рич-текст-класс на сайте:
правило на каждый тег, нормальная убывающая лесенка `h2 → h3 → h4 → h5 → h6`, оверрайды на
991 / 767 / 479, и в нём уже стоит `h4 { max-width: none }` — глобальная грабля в 400px обезврежена.

| | `.birchcourse-rich-text` | `.legal-rt` |
|---|---|---|
| `p` | 0,9rem / **1,4em**, вес 500, `wght 540`, mb 0,75rem | 0,9rem / **1,2**, вес **600** |
| `h2` | 1,4rem / 1,0, ls −0,02em, m 1,875rem / 0,75rem | нет |
| `h3` | 1,2em / 1,3, ls −0,02em, `max-width: none` | 30px / 1,3 |
| `h4` | 1em / 1,3, `max-width: none` | нет |
| `h5` | 0,9em / 1,5 | нет |
| `h6` | 0,833em / 1,4 | нет |
| `ul` / `ol` / `li` | padding-left 25/32px, li 1,4, mb 0,375rem | нет |
| `blockquote` | плашка light-gray, радиус 0,5em, 22px | нет |

Кегль тела у курса и у юр-страниц **одинаковый** (0,9rem). Разница только в интерлиньяже и весе:
юр-страницы это «курс со сломанной настройкой».

Кейсы (`.cs-rich`) как донор хуже: класс прописан беднее, и он заточен под короткие абзацы кейса.
Блог (22px) — настройка «читать подряд», а юр-документ сканируют.

**Решение (22.09.2026): база + комбо, один общий класс на оба раздела.**

- `.birchcourse-rich-text` переименовывается в нейтральное `rich-text-base`; все правила по тегам
  остаются в нём — единственный источник правды для интерлиньяжа, весов и лесенки заголовков.
- Курс — `rich-text-base rt-course`, юр-документы — `rich-text-base rt-legal`.
- В комбо только расхождения. У `rt-legal` это `max-width: 680px`, и пока всё.

Проверено перед решением: `birchcourse-rich-text`, `legal-rt` и `centered-terms` не упоминаются
ни в одном инлайновом `<style>` или скрипте ни на одной из проверенных страниц (курс, styleguide,
блог, юр-страница) — значит на них не завязан ни страничный, ни сайтовый кастом-код, и
переименование безопасно. Прецедент комбо в этом классе уже есть: `.birchcourse-rich-text.mob`.

Переименование класса — операция Designer; в MCP rename не найден (уточнить при сборке шаблона).

**Колонка сужается с 800 до 680px.** У курса абзацы короткие, а у DPA тело 62 КБ; при 800px строка
выходит ~105 знаков при комфортных 70–80.

Писать стили **тег-стилями внутри класса** (`.legal-rt h5`), как сделано у `.cs-rich`: через
`data_style_tool` с `style_name: "h5"` + `parent_style_names: ["legal rt"]` (имя родителя как в
Designer, с пробелом). Подробности — `docs/webflow-mcp.md`.
