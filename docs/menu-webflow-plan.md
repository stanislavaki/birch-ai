# Меню v2 → Webflow: план переноса

## ⚑ ЗАПУЩЕНО НА ПРОДЕ (28.08.2026)

Хирургия выполнена, `bir.ch` на новом меню. Свип по всем 314 URL sitemap: **282 страницы — новый нав** (по 10 строк сабменю в каждой раскладке), темы точно как до операции (270 светлых `inverted-bg`, 12 тёмных, 2 легаси `white` → светлые через алиас), 29 легаси-навов и 3 страницы без нава — не тронуты, как и планировалось. Бэкап: компонент `super-navigation-backup` (id `271b1237-b47b-bad1-3e07-f07cb9b8adc0`).

**Главное архитектурное изменение по ходу: сабменю СТАТИЧНОЕ, не CMS.** Webflow запрещает Collection List внутри компонента, который стоит на CMS-шаблонных страницах («you can't add a Collection List to a Component that's used on page») — а `super-navigation-component` стоит на шаблонах блога, кейсов, курса. Решение (обсуждено, выбран вариант «статика»): 20 строк (10 десктоп + 10 мобилка) — обычная разметка в компоненте; New/Soon — элементы с классами `nav2__new`/`nav2__soon`; порядок = DOM-порядок; «скрыть пункт» = visibility элемента в Designer. Коллекция `Nav submenu items` осталась как источник контента, **в рендере не участвует** — удалить её (и её noindex-шаблон) хвостовой задачей после того, как меню устоится.

**Как устроена тема после хирургии (важно для будущих правок):** проп `color-mode` НЕ перепривязывался. Он как и раньше пишет значение в class-атрибут обёртки `.nav-inner`, а новый нав лежит ВНУТРИ неё — поэтому тема приходит как класс-предок, и CSS ловит её селекторами `.inverted-bg .nav-v3, .white .nav-v3, .blog .nav-v3`. `.nav-v3.is-light` остался ручным dev-переключателем (`?nav3theme=light`). С корня `<nav>` сняты легаси-классы `nav/Navigation/Navigation 2` — иначе `.nav { position: fixed; height: var(--nav-height); width: 100% }` оставлял бы невидимую полосу во всю ширину, перехватывающую клики по бокам от пиллы.

**Перепривязано через API** (биндинг на существующие пропы работает, вопреки ожиданиям): `Get started url` → обе CTA (проверено: `/hub` сохранил `?gateway=true`), `mobile page title` + `show mob title` → скрытый плейсхолдер, `custom logo image` + оба `show custom logo…` → скрытые Image-плейсхолдеры. **Не удалось через API:** биндинг class-атрибута на проп (ошибка «value must be a string or a binding») — обошли вложенностью в `.nav-inner`, см. выше.

**Поиск** перенесён (не пересобран) в пиллу: привязка пропа `Search field`, класс-контракт `.search-blog` и IX2-интеракции живы — проверено на `/blog`, индекс Fuse.js грузится, выдача фильтруется. Гоча раскладки: пилла — `flex` со `space-between`, из-за чего перенесённый блок уезжал в центр и перекрывал меню; лечится `margin-right: auto` (прижать к лого). **Известный компромисс: на ≤991px поиск скрыт** — слева от центрированного меню нет места. Финальное размещение/дизайн поиска — задача арт-диру.

**Инцидент 28.08 и фикс.** Сразу после выката на всех страницах контент подъехал вверх на высоту меню (шапки и sticky-табы Terms ушли под пиллу): при хирургии с корня были сняты легаси-классы, а `.nav { height: var(--nav-height) }` держал резерв высоты в потоке. Починено классом `nav-v3-root` (`width:100%; height:6.5rem`; `small` → `5.5rem`) — в rem, чтобы совпадать с rem-геометрией пиллы (в em при fluid-руте резерв выходил на 5px короче низа пиллы). Проверено визуально на главной, Terms, /features, /blog, /pricing и мобилке; свип: резерв есть на всех 282 страницах. Подробный разбор гочи — в `docs/webflow-mcp.md`.

**Поиск блога после переноса (28.08).** Симптом «поиск не ищет» оказался не поломкой фильтрации: Fuse грузится и фильтрует корректно, но раньше при фокусе срабатывала Webflow-интеракция, схлопывавшая блок-герой `.search-shrink-block`, и результаты вставали прямо под навом. После переноса поля в пиллу интеракция перестала срабатывать → герой оставался, результаты рендерились ~1300px ниже, экран не менялся. Воспроизведено кодом: отдельный embed сразу после JS-нава прячет `.search-shrink-block`, пока в поле есть запрос. **Важно про тайминг:** этот embed сидит в наве в самом начале `<body>`, поэтому `querySelector('.search-shrink-block')` на момент выполнения возвращает `null` — слушатель обязан быть делегированным на `document` (первую версию с прямым запросом пришлось переписать).

**Поиск блога — финальный паттерн (по историческим макетам Figma `blog-layout`).** Десктоп (узлы 525-1043 / 525-1309): компактное поле всегда видно рядом с лого; по фокусу разворачивается на всю пиллу, центральное меню и группа CTA уходят, появляется крестик; сворачивается по крестику, Esc или уходу фокуса из пустого поля. Мобилка (541-3465 / 541-3752): в пилле лупа → тап разворачивает поле поверх пиллы, бургер остаётся; крестик очищает и сворачивает. **Один визуальный враппер:** бар — единственный видимый контейнер (белая заливка, без обводки), собственные фон/рамки легаси-поля (`.search-form-block`, `.search-form`, `.search-flex`, инпут) погашены, иначе получалась коробка в коробке. Компактное поле прижато к лого через `margin-right: auto` — иначе `space-between` уносит его в центр, под центрированное меню.

**Анимация разворота — три грабли подряд.** (1) `width: 250px → auto` не интерполируется: ширина щёлкала мгновенно, а высота ехала 220ms — получался рывок. Оба состояния должны быть явными длинами, и **все** меняющиеся свойства (width, height, padding, border-radius) — в списке `transition`. (2) Развёрнутая ширина = `100%` контентбокса пиллы, что на ширину лого больше доступного места; поэтому `flex-shrink` оставлен включённым (`flex: 0 1 auto`) — флексбокс ужимает поле до свободного места, и анимация визуально доезжает чуть раньше конца таймлайна. С `flex: none` было бы переполнение вправо. (3) `margin-right: auto` держится **в обоих** состояниях: в компактном прижимает к лого, в процессе разворота съедает убывающее свободное место. Если убрать его в развёрнутом, во время анимации это место достаётся `space-between` и растущее поле уезжает вправо. Итог замера: левый край стоит на месте (198px в обоих состояниях), правый едет 473 → 1390 внутри пиллы. Классы: `nav2__searchbtn` / `nav2__searchbar` / `nav2__searchclose` и мобильные близнецы `nav2m__*`; состояние — класс `is-searching` на `.nav2` / `.nav2m`. Оба набора привязаны к существующему пропу `Search field`, поэтому существуют только там, где он включён (/blog). Десктопное поле — это перенесённый старый `search-form-block` (сохранены привязка пропа, класс-контракт `.search-blog` и IX2), мобильное — отдельный инпут `#nav-search-mobile` (скрипт блога сам биндится ко всем `.search-blog`). Инсет крестика параметрический: `(высота бара − высота кнопки) / 2`, поэтому поля вокруг него одинаковые. **Побочный выигрыш: overlay не конкурирует с центрированным меню, поэтому правило «прятать поиск ≤991px» снято — поиск работает на всех ширинах, включая планшеты.**

**Ссылки сабменю.** Пользователь перепривязала строки через page-picker в Designer (`/launcher` удалён — страницы нет, «Prompting guide» → `/how-to-choose-birch-ai-tools`). Проверено на паблише: **ссылка из пикера побеждает мой литеральный `href`-атрибут из whtml** — в разметку попадает только она, чистить атрибуты не требуется.

**Отступы под меню.** `.container.intro` (главная, /ai, /hub, /mcp) считает верхний паддинг формулой `calc(var(--nav-height) + Nrem)`; на мобилке N уменьшен 3 → 2 (−16px, 127 → 111px). Десктоп не трогаем: там новое меню на 15px ниже старого.

**Мобильные первые экраны — единый зазор 44px.** Метрика: от низа пиллы (76px: 12 margin + 64 высота) до верха первого элемента. Число выведено из блога — там верх сравняли с низом (низ 62), получилось 64; затем арт-дир посмотрела `_1100` на 32px, что даёт 44, и приняла 44 как общий стандарт. **Исключения, оставленные сознательно: блог (64) и `.container.intro` (главная, /ai, /hub, /mcp).** Все правки breakpoint-scoped на `small` (десктоп и планшет нетронуты), значения ниже — измерены на 375px после публикации.

| страница | было | стало | ручка |
|---|---|---|---|
| /integrations | 198 | 44 | `.integration-hero-section` margin-top 8rem → **−26px** |
| /case-studies | 172 | 44 | `.nd-section.cases` padding-top → 0 **и** `.old-container._1200.fw` margin-top 80 → 32px |
| /demo, /legal/*, /experts, /affiliates, /channel-partners, /slack, *-alternative | 166 (demo) | 44 (demo, legal) | `.old-container._1100` margin-top 114 → 32px |
| /pricing | 143 | 44 | `.section-hero.c.pric` padding-top → 21px **на `small` и на `tiny`** |
| /explorer | 143 | 44 | `.hero-block-no-scroll` margin-top 4.1rem → **−33px** |
| /stage | 140 | 44 | `.h1-main-stage-img-mob` margin-top 8rem → 2rem |
| /blog | 132 | **64** | `.blog-top-mob` (имя стиля — «blog top mob») margin-top 6rem → 1.75rem |
| детальная кейса | 115 | 44 | `.gap-top-nav` (спейсер, только этот шаблон) margin-top → 0, height `var(--nav-height)` → 2rem |
| /features (108), страница поста (108) | — | не трогали | уже были плотнее |

Что стоит помнить:
- **375px — это `tiny`, а не `small`.** Брейкпоинты Webflow: main 992+, medium ≤991, small ≤767, tiny ≤479. Правка на `small` наследуется в `tiny`, **но только если в `tiny` нет своего override**. У `.section-hero.c.pric` он был (те же 120px) — пришлось писать в оба.
- **Имена стилей в Webflow могут быть с пробелами, а классы с цифры получают `_` только в селекторе.** Стиль «blog top mob» → `.blog-top-mob`; стиль «1100» → `.old-container._1100`. Поиск по `name_path` идёт **по имени, не по селектору**, поэтому `["old-container","_1100"]` не находит ничего, а `["old-container","1100"]` — находит.
- **Единую величину нельзя задать одним классом.** У каждой страницы своя цепочка отступов, и общая ручка даёт разный результат: после правки `_1100` (11 страниц) demo и legal встали на 44, а experts 104, slack 84, affiliates 124, channel-partners 174 — у них сверху лежат собственные отступы. Единой может быть только цель (зазор), выставлять её приходится постранично.
- **Отрицательные margin — рабочий вариант там, где ручка не дотягивает.** На /explorer (`justify-content: space-around` в герое) и /integrations остаток шёл из внутренней раскладки, куда паддингом не достать. `−33px` и `−26px` на page-scoped классах дали ровно 44 без наложения на пиллу. Оба класса проверены краулом: встречаются только на своей странице.

**Мобильная пилюля в светлой теме — `rgba(211,209,224,0.9)` вместо прототипных `0.4`.** Проверено экспериментом на белой странице: если убрать заливку и оставить только блюр, пилюля **исчезает полностью** — над плоским белым `backdrop-filter` не даёт ничего (размывать нечего, `brightness` белое ярче не сделает). В прототипе фон светлой темы `#FBFBFD` и под пилюлей всегда есть контент, поэтому случай не всплывал. Оговорка: в Chrome прототипные `0.4` читаются нормально — жалобу с айфона воспроизвести не удалось, так что 0.9 это подстраховка, а не доказанная необходимость.
**Хвосты:** ревью арт-дира (шрифты/стекло), дизайн поиска, легаси-навы (13 × `/tag/*` — один шаблон Tags Template, 13 × `/experts/*`, `/all`, `/facebook-ads-automation/lp0`), удаление мёртвого `initMobileMenu` из site-JS, удаление коллекции `Nav submenu items` и бэкап-компонента после стабилизации.


Задача: перенести финальный dev-прототип нового меню (`menu.html`, PR #19) в Webflow.
Источник правды — **прототип**, не Figma (макет ниже по течению, часть решений принята позже него).
Референсы: витрина `menu-showcase.html`, ревью-артефакт https://claude.ai/code/artifact/8f0fd5e5-47b0-4edd-9260-3fdfaa84eaae,
контекст решений — память проекта (`project_menu_v2.md`).

## Статус (28.08.2026)

Шаги 1–3 выполнены через Webflow MCP, обкатка на стейджинге:

- **Шаг 1 (quick-win)** ✅ — в `super-navigation-component`: `data-cta-type="nav-sign-up-mobile"` на мобильной Get started, `aria-label="Menu"` на бургере. **Важно про механику трекинга**: site-level JS (`initCtaTracking` в футер-коде сайта) вешает клик-трекинг на элементы с классом **`.cta-track`**, а `data-cta-type` — лишь поле в payload события. Поэтому атрибута мало — мобильной Get started добавлен и класс `cta-track` (`set_style ["btn","cc-nav","cta-track"]`, комбо уже существовало у десктопной). В новом наве `cta-track` навешан комбо-классами на Book demo (`nav2__txtbtn.cta-track`), Get started десктоп (`nav2__cta.cta-track`) и мобайл (`nav2m__cta-main.cta-track`). Уже на стейджинге; на прод уйдёт с ближайшим Publish.
- **Шаг 2 (CMS)** ✅ — коллекция `Nav submenu items` (id `6a9179d51fb9675cc2c7bf93`), все поля с help-текстами, 10 итемов из прототипа залиты и опубликованы (первый `publish_collection_items` дал 404 — коллекция была моложе последнего Publish; ушли с полным Publish стейджинга).
- **Шаг 3 (сборка)** ✅ с одним отступлением — **нав собран как page-level дерево `nav.nav-v3` на noindex-странице `/menu-dev`**, а не как компонент: `transform_element_to_component` не принимает деревья с Collection List, а фильтры листов внутри определения компонента через API не записываются (детали — `docs/webflow-mcp.md`, раздел «Компоненты и Collection List через MCP»). **«Create component» из готового дерева — одно ручное действие в Designer** (выделить `nav-v3` → Create Component → назвать `navigation-v3-draft`); после него всё продолжает работать, листы в компонентах Webflow поддерживает.
- Архитектура внутри: embed `nav-custom-css` (весь CSS + `:root { --nav-height: 6.5em }` + мобильный скоуп 5.5em — **определение переменной теперь едет с компонентом**, в старом компоненте оно живёт в его embed'е), разметка на классах `nav2*`/`nav2m*` (имена 1:1 с прототипом), 4 Collection List (Product/AI × десктоп/мобилка; фильтр Group+Show, сортировка Order), JS-embed поведения. Светлая тема — комбо `is-light` на корне (под привязку `color-mode` при хирургии); состояния для дизайнера — пре-минченные комбо `is-open`/`is-expanded`. Жёлтая мобильная CTA — отдельный класс `nav2m__cta-main` (не комбо `is-yellow`).
- **Стейджинг**: https://birch-testing.webflow.io/menu-dev (тёмная), `?nav3theme=light` — светлая. Проверено в браузере: обе панели с ховер-плашкой, New/Soon чипы (Soon-строка задизейблена), мобильная шторка с аккордеоном, бургер 2→4 точки, логотип, обе темы.
- **Трекинг сабменю включён** (по команде пользователя): `cta-track` + `data-cta-type="nav-submenu"` / `"nav-submenu-mobile"` на шаблонных ссылках всех 4 Collection List — один шаблон покрывает все пункты, событие само несёт `cta_text` (название пункта) и `cta_destination` (URL). Soon-строки не кликаются, событий не дают.
- **Дев-параметры и a11y**: `?nav3active=/features` симулирует активную страницу (точки в наве — для ревью без ухода со стейджинга), активные ссылки получают `aria-current="page"`, Soon-строки — `aria-disabled="true"`. На белой секции — sticky-проба `top: var(--nav-height)`: пинится ровно под пиллой (проверено), мобильный скоуп 5.5em проверяется там же. Гоча: sticky внутри секции с `overflow: hidden` не работает — секциям поставлен `overflow: clip` (клипает, но не создаёт скролл-контейнер).
- **Окружение dev-страницы** приближено к бою: компоненты `Fluid Type` (резиновый root font-size — без него rem-скейл нава не как на проде), `Global Desinger Custom Code` (font-smoothing и глобальные модификаторы) и `Global / Footer Black`; между навом и футером — 4 тестовые секции для стекла (тёмная / белая с точечной сеткой и блобами / цветные плитки / чёрная со штрихами). Site-level кастом-код сайта на нав не влияет (проверено: все правила и скрипты целятся в чужие классы; `initMobileMenu` старого нава внутри нового не находит своих элементов и молча выходит).

**Компонент создан (28.08, руками в Designer):** `navigation-v3-draft`, id `c20f1e63-de86-9ba7-ad51-2a8f4f3f9a83`, инстанс на `/menu-dev`. При упаковке Webflow перевесил conditional-visibility чипов на авто-пропы (см. гочу в `docs/webflow-mcp.md`) — починено руками: все 8 чипов на условиях New is On / Soon is On, мусорные пропы Override удалены; авто-пропы Text 1–7 / Image 1–2 оставлены (Image — на плейсхолдерах замороженных фич). Сверено по паблишу: 20 CMS-строк, чипы только у Bïrch MCP (New) и Routines (Soon), фильтры/сортировка/биндинги целы. **При хирургии повторить эту проверку — копирование шаблонов листов между компонентами может снова перевесить visibility.**

Дальше по порядку работ: шаг 4 — ревью арт-дира на стейджинге, шаг 5 — хирургия с бэкапом, шаг 6 — легаси.

## Финальные спеки (из прототипа)

- **Высоты** (`--nav-height` остаётся источником правды для sticky/embeds, ничего не переписываем):
  - десктоп: воздух 6 + пилла 92 (5.75rem) + воздух 6 = `--nav-height: 6.5em` (не меняется);
  - мобилка: пилла с полями 12px; компонент скоупит переменную: `@media (max-width:767px) { :root { --nav-height: 5.5em } }` → пины на 12px под пиллой.
- **Стекло** (пилла, панель, мобильная карточка = один материал):
  - fill тёмная `rgba(63,63,63,.6)`, светлая `rgba(211,209,224,.4)` (#D3D1E0);
  - backdrop: `blur(30px) saturate(0.8) brightness(1.15)` — пилла и панель одинаково;
  - мобильная открытая карточка НЕпрозрачная: тёмная `#292929`, светлая `#fff`; пилла в открытом состоянии красится в цвет карточки; scrim за карточкой: тёмная `rgba(13,13,18,.6)`, светлая `rgba(255,255,255,.5)` + blur 12.
- **Геометрия десктопа**: корпус пиллы = контентная колонка `.container` (80rem, гаттеры 1.5rem) + рамка-инсет (высота пиллы − высота кнопки)/2 = 10px; лого и Get started стоят на краях контентной сетки; панель 30.75rem, паддинг 8, радиус 20 (концентрично 12+8), колонка заголовков 10.5rem, строки py 16, панель выровнена по левому краю текста триггера.
- **Типографика**: тексты меню 0.9rem/14.4 (как на бою); трекинг ярусами: 24px → 0em, 16px-ярус → 0.01em, 12px → 0.01em; вес 600; CTA-текст оптически центрован по полосе cap-height (на 56px кнопке = padding-bottom 1px).
- **Ховеры**: общий токен — белый 10% (тёмная) / белый 70% (светлая); в сабменю плашка-слайдер ездит между пунктами (320ms cubic-bezier(.22,.9,.24,1)), описание набирает контраст; Book demo/Log in — плашки впритык (текстовый зазор 28 = зазору пунктов); Get started: чёрный текст, ховер indigo #4450F2 + белый.
- **Теги**: New — жёлтый, rotate −18°, 0.7rem; Soon — тот же стиль в приглушённых цветах (`--nav-soon-*`); зазор заголовок↔тег 0.2rem; отрицательный margin-bottom −0.35em (теги не раздувают строку); disabled-пункт: приглушение + `pointer-events:none`.
- **Мобилка**: карточка-враппер 12px от краёв, влетание снизу (translate, 380ms cubic-bezier(.32,.72,0,1), радиусы сохраняются), аккордеон «один открыт», автораскрытие раздела активной страницы + активный пункт в зоне видимости, бургер 2→4 точки и нейтральный круг в открытом состоянии (белый/`#DBDAE2`), кнопки-чипы серые (`#3F3F3F`/`#DBDAE2`), зазоры кнопок 8, до краёв 12, кегль CTA 24.
- **Активные состояния**: точка у пункта (по слову, не по боксу) и у пункта сабменю; URL-матчинг по префиксу (`/case-studies/*`), у групп — список URL.
- Контент финальный — уже в прототипе (тексты Даши, Routines = Soon без ссылки).

## Архитектура в Webflow

- **Верхний уровень** — компонент `super-navigation-component` (id `33e783c9-057c-0437-6c4c-ea28f08e4679`, 78 инстансов). Пропы ПЕРЕИСПОЛЬЗУЕМ (не создаём новые!) — тогда переживут замену все пер-страничные оверрайды (напр. hub: `?gateway=true`). Ключевые: `Get started url`, `color-mode` (тема страницы), mob title. MCP пропы не умеет — привязка руками в Designer (~5 шт.).
- **Сабменю** — CMS-коллекция `Nav submenu items`: Name, Description, URL, Group (Product|AI), Order, Show (switch), New (switch), **Soon (switch)** → два Collection List (панель десктопа + мобильный аккордеон). Наполнение — 10 пунктов из прототипа.
- **Переменная**: `--nav-height` 6.5em + мобильный скоуп 5.5em — в nav-custom-css самого компонента.
- **JS-поведение** (ховер-интент, плашка-слайдер, аккордеон, влетание, active-матчинг) — embed внутри компонента; классы `.is-open` для дизайнера (открытые состояния видны на холсте).

## Хирургия (шаг 5): карта перепривязки пропов

Данные — из crawl'а всех 314 URL sitemap (28.08.2026) + чтения биндингов определения компонента.

**Перепривязать (руками в Designer):**

| Проп | В старом наве | В новом наве | Оверрайды на бою |
|---|---|---|---|
| `color-mode` (string) | значение = доп. класс на `.nav-inner` (биндинг на class-атрибут) | class-атрибут корня `.nav-v3` | **дефолт `inverted-bg` = СВЕТЛЫЙ режим** (270 стр.; `.inverted-bg` переопределяет `--colors--text` на чёрный и т.д.); `""` = тёмный (12 стр.: главная, features, stage, hub, ai, mcp, manage, strategies, integrations, birch-course, ad-quantum-guide, pills-design) |
| `Get started url` (link) | href обеих Get started | link-настройка `.nav2__cta` и `.nav2m__cta-main` | 1 стр.: /hub → `?gateway=true` |
| `mobile page title` + `show mob title` | текст/видимость `.mob-heading` в пилле | **заморожено (решение 28.08)**: скрытый плейсхолдер `.nav2m__pagetitle` (display:none) в мобильной пилле — перепривязать текст+видимость на него; данные всех 222 стр. живут, дизайн решаем позже | 222 стр. показывают тайтл (190 блог-постов «Blog», 20 стр. курса «Course», 12 продуктовых) |
| `custom logo image` + `show custom logo on desktop/mobile` | подмена лого | **заморожено (решение 28.08, вариант «а»)**: скрытые Image-плейсхолдеры `.nav2__customlogo` / `.nav2m__customlogo` в пиллах — перепривязать image+visibility; /hub временно показывает вордмарк (осознанный регресс до уточнения) | 1 стр.: /hub (кастом-лого) |
| `Search field` (bool) | видимость поиска (`search-form-block`) | **сохранить (решение 28.08, вариант «а»)**: при хирургии `search-form-block` НЕ пересобирать, а ПЕРЕМЕСТИТЬ целиком внутрь новой пиллы (move сохраняет привязку пропа, класс-контракт `.search-blog` со скриптом /blog и IX2-интеракции иконок) + спозиционировать CSS'ом; редизайн лупы под пиллу — отдельной задачей после ревью | 1 стр.: /blog (индекс); логика поиска — page-скрипт /blog (Fuse.js), читает инпут по классу `.search-blog` |

**Ключевой ход по `color-mode`:** в embed CSS нового нава добавить алиасы `.nav-v3.inverted-bg, .nav-v3.white { … }` = копия `.nav-v3.is-light` — тогда все 270 светлых страниц получают светлый нав автоматически, без единой ручной правки инстансов; тёмные 12 (пустое значение) остаются тёмными. Легаси-значение `white` покрывается тем же алиасом.

**Не переносятся (умирают со старой разметкой):** `link 1..6` / `3.1` / `3.2` (+тексты и url) — теперь статика+CMS; `book demo`/`Log In`/`CTA-text` тексты — статика (crawl расхождений текстов не показал); `bird-image`, `show bird`, `show "Birch" …` — в v3 нет птицы по решению дизайна.

**Решения по функциям старого нава (28.08, подтверждены пользователем):** мобильный page title — заморозка через плейсхолдер (допилим потом); кастом-лого — заморозка через плейсхолдеры, /hub временно с вордмарком; поиск — сохраняется переносом блока как есть. Плейсхолдеры уже стоят в драфте (скрытые, подписаны в Navigator: «… placeholder (bind prop, hidden)»).

## Шаг 6: инвентаризация легаси-навов (crawl 28.08.2026)

- **282 страницы** — на `super-navigation-component`: заменяются хирургией автоматически.
- **Легаси-навбары (не супер-нав), 28 страниц:**
  - 13 × `/tag/*` — CMS-шаблон Tags Template, старый w-nav навбар (правится один раз в шаблоне);
  - 13 × `/experts/*` — статические страницы Revealbot со старым w-nav навбаром;
  - `/all` и `/facebook-ads-automation/lp0` — старый нав с `.nav-inner.white` (это и есть живые «Navigation White»-страницы);
  - соотнесение с компонентами `Navigation` (1 инстанс) / `Navigation White` (6) — в Designer: часть легаси, судя по числам, лежит разлинкованной разметкой или на драфт-страницах.
- **Без нава (осознанно):** `/lp/revealbot-2wt`, `/signals-gateway`, стайлгайды.

## Порядок работ

1. **Быстрая победа (можно сразу)**: `set_attributes` в определении компонента — `data-cta-type="nav-sign-up-mobile"` на мобильную Get started (сейчас мобильные регистрации не трекаются) + aria на бургер.
2. **CMS**: создать коллекцию по схеме, залить 10 пунктов, Publish итемов.
3. **Драфт-компонент** `navigation-v3-draft` на noindex-странице `/menu-dev`: собрать разметку (whtml/element_builder по гочам из `webflow-mcp.md`), стили классами + переменные, JS embed. Обкатка на стейджинге, обе темы, мобилка.
4. **Ревью** прототипа в Webflow (арт-дир смотрит стейджинг).
5. **Хирургия** (между публикациями, окно — минуты): дубль `super-navigation-component` → `super-navigation-backup`; вставить новую разметку рядом со старой; перепривязать существующие пропы (руками, по списку); удалить старую; Publish; сверка curl'ом по опубликованным страницам.
6. **Хвосты**: легаси `Navigation` (1 инстанс) и `Navigation White` (6) — заменить и удалить; проверить светлые страницы (`color-mode`).

## Грабли, о которые уже известно (сверяться по ходу)

- Webflow-гочи сборки: `docs/webflow-mcp.md` (резолвинг имён классов, gap → grid-row/column-gap, breakpoint_id, `!important` только кастом-кодом, голый grid = 2 колонки и т.д.).
- **Предок с backdrop-filter глушит вложенный blur** → панель сабменю НЕ вкладывать в пиллу (сиблинг), анимацию появления — на самой панели (`docs/dev-pages.md`).
- **var() в значении кастом-свойства резолвится в месте определения** → темизацию строить на прямых ссылках у потребителей, без переменных-прослоек.
- `a { color: inherit }`-сбросы перебивают одноклассовые цвета → цвета ссылок компаунд-селекторами, проверять computed.
- Кастом-код не работает на холсте Designer → все открытые состояния должны включаться классом `.is-open` руками.

## Done =

Новое меню на всех 78 страницах с прода, обе темы, сабменю правится из CMS (включая Show/New/Soon), Get started сохраняет пер-страничные ссылки, sticky-блоки и embeds не тронуты, легаси-навы удалены, бэкап-компонент есть.
